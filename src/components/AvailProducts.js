import * as React from 'react';

const AvailProducts = ({availProductButton}) => {
  return (
    <div className="availProductButton">
      <button type="button">Available Now</button>
    </div>
  );
};

export default AvailProducts;