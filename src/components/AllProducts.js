import * as React from 'react';

const AllProduct = ({allProductButton}) => {
  return (
    <div className="allProductButton">
      <button type="button">All Products</button>
    </div>
  );
};

export default AllProduct;